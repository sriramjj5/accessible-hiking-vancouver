import { getTrailJson } from "./write-trail-json";
import { Loader } from '@googlemaps/js-api-loader';
// remember to not put public API key anywhere in the code. remove after testing

// writeTrailJson();

// const loader = new Loader({
//     apiKey: "",
//     version: "weekly",
// });

// loader.load().then(async () => {
//     const directionsService = new google.maps.DirectionsService();

//     let request = {
//         origin: "",
//         destination: "",
//         travelMode: 'DRIVING',
//     }

//     directionsService.route(request, function(result, status) {
//         console.log(result);
//         console.log(status);
//     })

// })

